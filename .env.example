PORT=3000
DATABASE_PATH=./data/idrem.sqlite
# Required for password-reset links and production origin checks.
PUBLIC_ORIGIN=
# Optional SMTP: password recovery remains unavailable until configured.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# Only used by npm run admin; remove ADMIN_PASSWORD after provisioning.
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=Hokage
